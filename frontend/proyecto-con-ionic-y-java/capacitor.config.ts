import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'proyecto-con-ionic-y-java',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
